#include <stdio.h>

struct student {
    char name[29];
    int semester;
    struct birthday {
        int day;
        int month;
        int year;
    };
};

int read_file (struct student *s, int max)
{
    
}

int main()
{
struct student studentlist[100];
}